# Entity: performance

main: kit: entity: performance: {

  alias: field: {}

  fields: [
    {
      name: "loadTime"
      req: false
      short: "Page load time in milliseconds"
      type: "`$NUMBER`"
    }
    {
      name: "pageSize"
      req: false
      short: "Total page size in bytes"
      type: "`$INTEGER`"
    }
    {
      name: "requests"
      req: false
      short: "Number of HTTP requests"
      type: "`$INTEGER`"
    }
    {
      name: "timestamp"
      req: false
      short: "Timestamp of the analysis"
      type: "`$STRING`"
    }
    {
      name: "url"
      req: false
      short: "The analyzed URL"
      type: "`$STRING`"
    }
  ]

  name: "performance"
  op: {
    load: {
      name: "load"
      points: [
        {
          args: {
            query: [
              {
                example: "https://example.com"
                kind: "query"
                name: "url"
                orig: "url"
                reqd: true
                type: "`$STRING`"
              }
            ]
          }
          method: "GET"
          orig: "/api/performance"
          parts: [
            "api"
            "performance"
          ]
          select: {
            exist: [
              "url"
            ]
          }
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
